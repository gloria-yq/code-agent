# Todo demo seed

This small CLI stores tasks in a JSON file.

```bash
python todo.py add "write tests"
python todo.py list
python -m unittest -v
```

For a Code Agent demonstration, ask it to add `--status pending|done|all` filtering to the
`list` command, keep `all` as the default, add tests, update this document, and run the tests.

